module.exports = {
  rules: {
    /* Errors */
    'no-catch-shadow': ['error'], // < v5.1.0 - replaced by "no-shadow"
    /* Disabled */
    'require-jsdoc': ['off'], // < v5.10.0 - no replacement
    'valid-jsdoc': ['off'], // < v5.10.0 - no replacement
  },
};
